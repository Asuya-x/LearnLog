

infrastructure:
+ **storage**
+ communication
+ computation

目的是各种抽象

Impl.
+ RPC, thread, concurrency


topic:
+ Performance:
	+ scalability 可扩展性
		+ 2x ccomputers --> 2x throughput
		+ 但是水平扩展的最后一定会遇到 db瓶颈问题
+ fault tolerance 容错
	+ availability 可用性
	+ recoverability
		+ nv storage(nonvolatile storage)
		+ replication
+ consistency 一致性
	+ --> strong consistency
	+ weak consistency